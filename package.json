{
  "name": "soundgineer",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-based Vital-style wavetable synthesizer (Web Audio + AudioWorklet + WebGL2)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  }
}
